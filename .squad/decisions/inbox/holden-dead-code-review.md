# Dead Code Review — Full Project Sweep

**Date:** 2026-04-08
**By:** Holden (Lead)
**Scope:** All directories — infra, agent, tools, scripts, eval, data, tests, root files
**Context:** Issue #54 follow-up. Prior cleanup already removed `infra/modules/openai.bicep` and `infra/modules/keyvault.json`.

---

## 1. CONFIRMED DEAD CODE — Safe to Remove

### `data/synthetic_context.json` — DEAD
- **What:** JSON file with fictional change events, decisions, runbooks, ownership data.
- **Evidence:** Zero references in any Python file, workflow, test, or config. `grep synthetic_context` returns nothing.
- **Why dead:** Work context data is hardcoded in `tools/work_context_stub.py` (the `_CHANGE_EVENTS`, `_DECISIONS`, etc. dicts). This JSON file was likely an earlier design that got superseded.
- **Action:** DELETE. No code reads it.

### `DOCKER_OPTIMIZATION.md` — DEAD
- **What:** Planning document about Docker image size optimization (dated 2026-04-06).
- **Evidence:** Only appears in `.dockerignore` (to exclude it from builds) and `.squad/` history docs. Not referenced by README, Dockerfile, workflows, or any code.
- **Why dead:** Planning artifact whose recommendations were implemented in the Dockerfile. The doc itself serves no runtime or CI purpose.
- **Action:** DELETE. Optimizations live in the Dockerfile; the planning notes are in git history.

### `customerfriendly-plan.md` — DEAD
- **What:** Internal planning/instruction document for the demo narrative and constraints.
- **Evidence:** Only referenced in `.dockerignore` (excluded) and `.squad/` history docs (cited as source). Not linked from README or used by any code.
- **Why dead:** Internal planning artifact. The plan content is implemented across agent/, tools/, eval/. The doc itself is vestigial.
- **Action:** DELETE. Content is in git history if needed. Or move to `.squad/` if Tammy wants to keep it as team reference.

---

## 2. POTENTIALLY DEAD — Needs Human Judgment

### `data/seed_data.sql` — GENERATED ARTIFACT, NEVER READ BACK
- **What:** SQL INSERT statements generated by `data/seed_telemetry.py:write_sql()`.
- **Evidence:** The `write_sql()` function is called inside `seed()` (line 368), and `seed()` IS called by tests. But no code ever reads `seed_data.sql` back. It's a write-only artifact.
- **Why potentially dead:** It was likely designed as a convenience for Azure SQL manual import (the `seed_connection()` path handles SQLite directly). If nobody uses the SQL file for Azure SQL setup, both `write_sql()` and `seed_data.sql` are dead.
- **Action:** ASK TAMMY — Is anyone using `seed_data.sql` for Azure SQL seeding? If not, delete the file and consider removing `write_sql()` from `seed_telemetry.py`.

### `infra/main.json` — LOCAL ARTIFACT, ALREADY GITIGNORED
- **What:** Compiled ARM template output from `az bicep build`.
- **Evidence:** Was tracked previously, removed in commit `d9f228a`. Now gitignored by `infra/**/*.json` rule. Exists on disk only as local build artifact.
- **Status:** Already handled. Not a repo concern. Can be deleted locally.
- **Action:** NO REPO ACTION needed. Already gitignored. Delete locally if it bothers you.

---

## 3. FEATURE-FLAGGED CODE — Intentionally Kept

### `tools/work_context_mcp.py` — FEATURE-FLAGGED (ENABLE_MCP)
- **What:** MCP server wrapper around `work_context_stub.py` functions.
- **Flag:** `ENABLE_MCP` (default: OFF). Module exits early at line 36 when flag is disabled.
- **Evidence:** Has dedicated test file (`tests/test_work_context_mcp.py`), properly tested, clean implementation.
- **Action:** KEEP. This is intentional feature-flagged code, not dead code.

### `tools/work_context_stub.py` — FEATURE-FLAGGED (ENABLE_WORK_IQ)
- **What:** Synthetic work context data (change events, ownership, decisions, runbooks).
- **Flag:** `ENABLE_WORK_IQ` (default: ON). Gated in `agent/agent.py` and `scripts/run_local.py`.
- **Evidence:** Actively used by agent, demo mode, and all related tests.
- **Action:** KEEP. Active and tested.

---

## 4. CLEAN — No Dead Code Found

### `agent/` directory
- **agent/agent.py** — All functions called. No unused imports. No unreachable code.
- **agent/config.py** — All functions called. Clean.
- **agent/tracing.py** — All 9 functions used. All imports used.
- **agent/__init__.py** — Minimal, clean.

### `tools/` directory
- **tools/sql_telemetry.py** — All functions used (agent toolset, demo mode, tests). All 13+ functions called.
- **tools/action_stub.py** — All functions used (agent toolset registration, tests). Clean.
- **tools/__init__.py** — Minimal, clean.

### `scripts/` directory
- **scripts/run_local.py** — Active entry point, called in development.
- **scripts/setup_local_db.py** — Called in CI workflow (`ci-eval.yml` line 46).
- **scripts/run_regression_demo.py** — Actively tested (`test_regression_demo.py`).
- **scripts/export_dashboard_data.py** — ⚠️ Initially looked dead, but IS referenced by `docs/pages/dashboard.html` (line 237 error message tells users to run it, line 223 fetches the output file). Output file `docs/pages/assets/dashboard-data.json` exists. This is a manual workflow tool for the GitHub Pages dashboard. NOT dead.

### `eval/` directory
- **eval/evaluators.py** — All 4 evaluator classes in `EVALUATORS` dict, used by `run_eval.py`.
- **eval/run_eval.py** — Called in CI (`ci-eval.yml` line 52).
- **eval/testset.jsonl** — Loaded by `run_eval.py` (line 45).
- **eval/baseline_results.json** — Loaded by `run_eval.py` when `--compare-baseline` (line 47).

### `data/` directory
- **data/seed_telemetry.py** — Actively imported by setup scripts, run_local.py, conftest.py, and 3 test files.

### `infra/` directory
- **infra/main.bicep** — Source of truth, referenced by `deploy.sh`.
- **infra/keyvault-prereqs.bicep** — Active pre-flight template, 3+ references in `deploy.sh`.
- **infra/parameters.json** — Active deployment parameters.
- **infra/deploy.sh** — Active deployment script.
- **infra/modules/** — All 6 modules referenced by `main.bicep` or `keyvault-prereqs.bicep`.

### `tests/` directory
- All 15 test files import existing modules/functions (verified all import targets exist).
- All 9 conftest.py fixtures are used by tests.
- `test_agent.py` has an intentional shim (lines 22-29) wrapping `get_full_context` → `get_work_context` to bridge an API name mismatch. This is a known workaround, not dead code.
- No orphaned test files — every test file maps to an active source module.

### Root files
- **agent.yaml** — Active. Referenced in README.md, used by Azure AI Foundry deployment.
- **Dockerfile** — Active. Referenced in agent.yaml, CI/CD.
- **README.md** — Active documentation.
- **pyproject.toml / requirements.txt** — Always active.

---

## Summary

| Item | Verdict | Action |
|------|---------|--------|
| `data/synthetic_context.json` | ❌ DEAD | DELETE |
| `DOCKER_OPTIMIZATION.md` | ❌ DEAD | DELETE |
| `customerfriendly-plan.md` | ❌ DEAD | DELETE (or move to .squad/) |
| `data/seed_data.sql` | ⚠️ POTENTIALLY DEAD | Ask Tammy — is anyone using this for Azure SQL? |
| `infra/main.json` | ✅ ALREADY HANDLED | Gitignored, local-only artifact |
| `tools/work_context_mcp.py` | 🔵 FEATURE-FLAGGED | KEEP (ENABLE_MCP) |
| Everything else | ✅ CLEAN | No action needed |

**Bottom line:** 3 files confirmed dead, 1 needs human judgment, 1 already handled. The rest of the codebase is clean — no dead functions, no unused imports, no orphaned modules. The prior cleanup (openai.bicep, keyvault.json) caught the infra debt; this sweep catches the remaining data/doc artifacts.
