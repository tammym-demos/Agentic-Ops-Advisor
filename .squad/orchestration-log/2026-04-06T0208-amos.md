# Amos — Orchestration Log: 2026-04-06T02:08:00Z

## Task
Migrated `.github/workflows/deploy.yml` from azure-ai-projects v1 beta to v2 GA SDK.

## Work Done
- Replaced `AIProjectClient.from_connection_string()` with `AgentsClient(endpoint=..., credential=...)`
- Updated agent CRUD: `client.agents.*` → direct client methods (`client.list_agents()`, `client.create_agent()`, etc.)
- Rewrote thread/message/run handling: v2 SDK pattern with `client.runs.create_and_process()`
- Updated response extraction: `client.get_last_message_text_by_role(role=MessageRole.AGENT)` for cleaner filtering
- Renamed env var: `AZURE_AI_PROJECT_CONNECTION_STRING` → `AZURE_AI_AGENTS_ENDPOINT`
- Updated `.github/workflows/copilot-setup-steps.yml` to verify `azure.ai.agents` import

## Files Modified
- `.github/workflows/deploy.yml`
- `.github/workflows/copilot-setup-steps.yml`

## Commit
- `b417a2c` — fix: migrate deploy.yml to v2 SDK (AgentsClient)

## Status
✓ Complete. Ready for integration test phase.
